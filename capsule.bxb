capsule {
  id (jaehoon.epl)
  version (0.1.2)
  format (3)
  targets {
    target (bixby-mobile-ko-KR) {enabled (true)}
  }
  runtime-version (7)
  store-countries {
    all
  }
  device-models {
    all
  }
  capsule-imports{
    import (viv.core) {as (core)}
    import (viv.time){
      as(time) version (3.1.102)
    }
  }
  store-sections {
    section (Sports)
  }
}
